// funcion:

let suma = function(a, b){
    return a + b
    }

    resultado = suma(2, 1)
    console.log(resultado)

// arrow funcion:

let suma2 = (a, b) => a + b

result=suma2(2, 4)
console.log(result)
